class rev_char_traits: public std::char_traits<char> {
 public:
  static int compare(const char* s1, const *s2,
                     size_t length1, size_t length2) {
    while (length-->0) {
      while (

  }
};

typedef std::basic_string<char, rev_char_traits> RevString;

int main() {
  RevString s1 = "dcba";
  RevString s2 = "cba";
  return 0;
}
