class Point(object):
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def distance(self):
        from math import sqrt
        return sqrt(swlf.x * self.x + self.y * self.y)

p = Point(3, 4)
print p.distance()
